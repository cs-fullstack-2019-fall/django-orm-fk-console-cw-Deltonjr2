from django.urls import path

from . import views

urlpatterns = [
    path('', views.index, name='index'),
    urlpatterns = [

    path('title/' views.myCity, name=myCity)

urlpatterns = [
    path('admin/', admin.site.urls),
    path('route66',include'route66_app.urls'))
]